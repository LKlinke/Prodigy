# skip
# TODO how to do this with the CLI
nat x;
nat y;
nat c;
nat temp;

while( x > 0){
    y := 1;
    while (y = 1){
       {y := 0}[1/2]{x:= x+1}
       temp := 0
    }
    x := x - 1;
    c := c+1
}