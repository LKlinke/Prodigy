def main(){
    // we're using continuous distributions here, which aren't supported by pGCL
    x := uniformInt(0,30);
    y := uniformInt(0,30);

    // approx. pi/4 = 0.78539816339...
    return x^2 + y^2 <= 900;
}
